{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Narrow default capability: no blanket filesystem or shell permissions are granted.",
  "windows": ["main"],
  "platforms": ["linux", "windows", "macOS"],
  "permissions": ["core:default"]
}
