{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Mobile capability intentionally excludes desktop filesystem, shell, and local-runtime permissions.",
  "windows": ["main"],
  "platforms": ["android", "iOS"],
  "permissions": ["core:default"]
}
